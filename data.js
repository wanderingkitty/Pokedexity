import { getPokemon } from './ajax.js';

const activateListOfPokemons = document.querySelector('.logo-btn');
const pokemonContainer = document.querySelector('.pokemon-container');

activateListOfPokemons.addEventListener('click', async () => {
	
}